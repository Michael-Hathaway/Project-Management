import React from "react";

import SignOutButton from "./SignOutButton";

const NavBar = (props) => {
  return (
    <nav>
      This is the navbar
      <SignOutButton />
    </nav>
  );
};

export default NavBar;
